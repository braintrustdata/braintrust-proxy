# Deprecated module

The `proxy/` module is deprecated. Do not treat this directory as the source of
model routing, provider translation, request/response shaping, or gateway bug
fixes.

For model routing or translation bugs, search and edit the `gateway/` directory
in the parent Braintrust repository instead. Only change files in this module
when the user explicitly asks for work in the deprecated proxy code.
